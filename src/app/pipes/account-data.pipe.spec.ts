import { AccountDataPipe } from './account-data.pipe';

describe('AccountDataPipe', () => {
  it('create an instance', () => {
    const pipe = new AccountDataPipe();
    expect(pipe).toBeTruthy();
  });
});
